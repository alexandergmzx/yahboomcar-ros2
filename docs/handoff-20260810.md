# Night handoff — 2026-08-09 → 08-10 (branch `rviz-fun-20260809`)

Session ran under the CLAUDE.md unattended rules: session branch, append-only,
build+tests before every commit, isaac lock held around every isaac use, scratch
domain 67, **nothing pushed**, no domain-20 traffic.

## What landed (review, then merge/push at your discretion)

On `rviz-fun-20260809` (branched from `main` @ `b946b40`):

1. **`c9b4c9f` — the red frame is dead.**
   - `slam_debug.rviz` now anchors to `odom`: no more red "Frame [map] does not
     exist" during the minutes an Isaac boot needs to produce a map. SLAM
     corrections now show as the *map* stepping, not the scan (the honest picture,
     per rviz-guide signature 3).
   - New `yahboomcar_config/rviz/drive.rviz` — what `--fun` sessions open: a stock
     `ThirdPersonFollower` **chase camera** on `base_footprint`, fixed frame
     `base_footprint` (published by bringup's *static* TF, exists from the first
     second → the view structurally cannot sit red), scan as amber flat squares,
     map drawn behind at 0.6 alpha (nav2 convention), an Overhead ortho as the
     saved alternate view. Researched against the stock plugin set and nav2's
     default view; sources in the session log.
   - Gates: `colcon build yahboomcar_config` green; both configs held a live rviz2
     for 18 s with **zero** error lines; all anchor frames present within 4 s.
2. **`4e9112b` — feather boxes.** `arena_fun.usd` (0.02 kg boxes) built as a
   VARIANT next to the calibrated arena — calibration only ever reads
   `arena.usd`. `build_arena --out`, `YAHBOOM_ARENA_USD` in sim_runner, `--fun`
   auto-loads the variant and says so. Measured: **zone entry at 1.05 m/s — full
   commanded speed through first contact** — penetration to 0.07–0.17 m of the
   box centre (two runs). Physics verify PASS. Tests 28/28.
3. **`902600f` — "the car is slow" was partly the keyboard.** The vendor keyboard
   starts at 0.2 m/s; `q` raises 10% per press toward its 1.0 cap. The fun banner
   and teleop startup now say so.

Also still local on `main` (from before the unattended rules landed, committed
directly per the then-current convention): `28c3437` (--fun), `63a4fa8` (lidar
near-field fix), `b946b40` (nav2_smoke race) — `main` is 3 ahead of
`origin/main`.

## Morning decisions (parked, not decided)

1. **Raise the firmware speed cap?** `FIRMWARE_MAX_SPEED = 1.0` is an
   *assumption* (vendor keyboard defaults), shared by both backends on purpose.
   Making fun faster than 1.0 m/s means either measuring the real cap on the car
   or explicitly changing the assumption — contract territory, yours.
2. **Isaac encoder-yaw fidelity** (from 08-09): `/odom_raw` yaw ~3× true under
   turn slip vs the car's 7.7% → Isaac SLAM maps still not usable evidence.
   Options sketched in `docs/slam-research/isaac-scan-quality.md`.
3. **Residual ~10% scan dropout** on the Isaac lidar (real device: ~1.4%) —
   open finding, same doc.
4. **Push/merge**: `main`+3 and this branch are unreviewed by you; the night rule
   kept them local.

## Loose ends / honest notes

- The drive view's chase camera is verified to *load* (no error lines, view
  controller resolved); its actual look awaits your eyes — tune `Distance`/
  `Pitch` in the Views panel to taste, it saves back to the file.
- The first box-punch left a feather box wedged in the (1.45,1.45) corner of
  `arena_fun.usd`'s live session (boxes reset on every session start; nothing
  persisted).
- End state: no simulators, isaac lock released (verified process death), no
  domain-20 traffic all night. **Tree documented-dirty, one file:** `CLAUDE.md`
  carries YOUR uncommitted edit (the unattended-session rules themselves). It
  is your authorship and your call where it lands, so the night session
  followed it but did not commit it.

## Addendum — patrol-on-fun RViz chaos, fixed while you were out (day 2)

You reported `simctl patrol` on a fun session wrecking RViz. Two of the three
causes were my design debts; all fixed on this branch:

1. **`9707352` — the scan filter starved /scan once boxes moved.** It validated
   against the full arena *with boxes at home*; fun exists to punt them. New
   metric: walls-only, one-sided — a beam may return shorter than the wall (a box,
   wherever it is) but never longer (seeing through a wall = corruption). Five
   offline cases green (shoved boxes 0.000, the ±85° class 0.219 vs 0.10 gate).
2. **`e085a8c` — fun implies SLAM off.** Crash maps are garbage and their
   `map->odom` corrections are what yanked the view. Manual override printed.
   Plus a patrol hint (spectating wants the saved Overhead view) and the
   sim_patrol docstring caught up with the corner-box layout.

**Verified end-to-end (your exact scenario):** isaac fun session + patrol,
5 minutes, boxes flying: delivered /scan ≥12.3 Hz in every 30 s window,
pass-through 98.9–100%, patrol reached lap 11 uninterrupted, RViz alive
throughout, /map absent as designed. Honest caveat: exactly ONE stale-scan stop
mid-run (a corruption burst gapped past the 0.5 s rule once in 5 min) — the
class remains, vastly rarer; noted under the open ~10% dropout item. Normal
canonical-arena session: 100% pass-through. 2D regression green.
